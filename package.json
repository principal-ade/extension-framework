{
  "name": "@principal-ai/cline-extension-host",
  "version": "0.1.0",
  "private": true,
  "description": "A gRPC host for the Cline extension, providing workspace, window, and environment services.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate-protos": "ts-node ./scripts/generate-protos.ts",
    "clean": "rimraf dist generated"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.10.0",
    "@grpc/proto-loader": "^0.7.10"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "glob": "^11.0.3",
    "grpc-tools": "^1.12.4",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}